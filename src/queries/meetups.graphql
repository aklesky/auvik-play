subscription getMeetups($channel: String) {
  Meetups(channel: $channel) {
    rsvp_id
    mtime
    response
    venue {
      venue_name
      lon
      lat
      venue_id
    }
    event {
      event_name
      time
      friendly_date
    }
    group {
      group_country
    }
  }
}
